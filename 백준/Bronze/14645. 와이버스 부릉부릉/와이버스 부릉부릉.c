#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main() {
	int n, k, i, a, b;
	scanf("%d %d", &n, &k);
	for (i = 0; i < n; i++) {
		scanf("%d %d", &a, &b);
	}
	printf("비와이");
	return 0;
}